name: battery_monitor_app
description: Cross-platform Flutter companion app for the Battery Monitor.
publish_to: none
version: 0.3.9+18

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_reactive_ble: ^5.5.0
  permission_handler: ^12.0.3
  share_plus: ^13.3.0
  shared_preferences: ^2.5.5
  http: ^1.6.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
